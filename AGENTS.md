# AGENTS.md

This repository's agent guide is **`CLAUDE.md`** (Claude-Code-native). Any agent or tool that keys
off `AGENTS.md` should read `CLAUDE.md` first, then follow its doc-reading order.

- **Building LoopRR** (this repo): start at `CLAUDE.md`.
- **The operating loop** we run and sell: `docs/ultraloop.md`.
- **LoopRR's own runtime worker fleet** (the dev "dream team" a user configures): designed in
  `foundation/worker-team.md`; the canonical paste-ready routine prompts live under `docs/routines/`
  once generated.

Kept as a thin pointer so the repo works whether a session keys off `AGENTS.md` or `CLAUDE.md`.
